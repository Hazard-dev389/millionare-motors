PORT=3000
SESSION_SECRET=change_this_to_a_long_random_value
DEALER_USERNAME=dealer
DEALER_PASSWORD_HASH=      # leave empty; we'll create password in server (demo) if empty
SESSION_SECURE=false       # set true in production with HTTPS
